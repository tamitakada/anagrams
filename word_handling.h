#ifndef WORD_HANDLING

int is_duplicate(char * arr, char c);
char * generate_characters();
char * get_input();
int get_word_points(char * word);
int already_used(char * filename, char * word);

#endif